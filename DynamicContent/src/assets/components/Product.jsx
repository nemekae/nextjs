export const Product = ({productName, price}) => {
    return (
        <>
            <h2>The product name: {productName}</h2>
            <p>The product is sold for ${price}</p>
        </>
        
    )
}